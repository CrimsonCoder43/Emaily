name: MongoDB Keepalive
on:
  schedule:
    - cron: '0 3 * * 0'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keepalive endpoint
        run: curl -sf --max-time 60 "https://emaily-bala.onrender.com/api/keepalive?token=${{ secrets.KEEPALIVE_TOKEN }}"
